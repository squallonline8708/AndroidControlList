<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sdpw.squall.temp_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_wechat"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_wechat"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DialogActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScrollViewActivity" />
        <activity android:name=".SpinnerActivity" />
        <activity android:name=".DateTimeActivity" />
        <activity android:name=".ListViewActivity">

        </activity>
        <activity android:name=".CheckBoxFunctionActivity" />
        <activity android:name=".RadioButtonFunctionActivity" />
    </application>

</manifest>